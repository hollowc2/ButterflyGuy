# Existing direct ButterflyGuy settings (placeholders only)
SCHWAB_API_KEY=<schwab-app-key>
SCHWAB_SECRET_KEY=<schwab-app-secret>
SCHWAB_ACCOUNT_ID=<schwab-account-number>
SCHWAB_TOKEN_PATH=tokens.json
DATABASE_PASSWORD=<database-password>

# Compose interpolation value: put this in infra/.env as an absolute directory.
# Both Compose files refuse to start if it is absent; it is not a token value.
SCHWAB_GATEWAY_TOKEN_DIR=/absolute/path/to/schwab-token-directory

# Isolated read-only gateway foundation
SCHWAB_GATEWAY_BIND_HOST=127.0.0.1
SCHWAB_GATEWAY_PORT=8010
SCHWAB_GATEWAY_INTERNAL_KEYS_PATH=/run/secrets/schwab-gateway-keys.json
SCHWAB_GATEWAY_LOG_LEVEL=INFO
SCHWAB_GATEWAY_ORDER_WRITES_ENABLED=false

# Client-side gateway selection remains opt-in; production default is direct. For a
# live XSP canary, keep URL/API key/toggle in infra/.env (not the root application
# .env). The loopback URL below is for a host-side demo; the live XSP container uses
# the Docker DNS default from infra/docker-compose.yml.
SCHWAB_ACCESS_MODE=direct
SCHWAB_GATEWAY_URL=http://127.0.0.1:8010
SCHWAB_GATEWAY_API_KEY=<scoped-internal-api-key>

# XSP-only live shadow canary, read by Compose interpolation from infra/.env.
# SPX and NDX do not consume this variable.
SCHWAB_GATEWAY_SHADOW_READS_XSP=false
